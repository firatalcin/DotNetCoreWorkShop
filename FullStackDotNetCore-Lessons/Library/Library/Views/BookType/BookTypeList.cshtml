@model List<BookType>
@{
    ViewData["Title"] = "BookTypeList";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h3 class="text-center">Kitap Tipi Listeleme Sayfası</h3>
<a asp-controller="BookType" asp-action="Create" class="btn btn-primary" style="float:right">Yeni Ekle</a>
<table class="table table-striped">
    <thead>
        <tr>
            <th>#</th>
            <th>İsim</th>
            <th>Aksiyonlar</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
       {
            <tr>
            <th>@item.Id</th>
            <td>@item.Name</td>
            <td>                
                <a class="big-icon" asp-controller="BookType" asp-action="HardDelete" asp-route-id="@item.Id"><i class="bi bi-x-circle-fill"></i></a>
                <a class="big-icon margin-icon" asp-controller="BookType" asp-action="Edit" asp-route-id="@item.Id"><i class="bi bi-pencil-square"></i></a>
            </td>
        </tr>
       }             
    </tbody>
</table>
