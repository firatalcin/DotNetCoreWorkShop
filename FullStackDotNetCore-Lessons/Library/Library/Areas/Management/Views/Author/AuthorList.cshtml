@model List<Author>


<h3 class=text-center>Yazar Listeleme Sayfası</h3>
<a asp-controller="Author" asp-action="Create" asp-area="Management" class="btn btn-primary" style="float:right">Yeni Ekle</a>
<table class="table table-striped">
    <thead>
        <tr>
            <th>#</th>
            <th>İsim</th>
            <th>Soyisim</th>
            <th>Aksiyonlar</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <th>@item.Id</th>
                <td>@item.FirstName</td>
                <td>@item.LastName</td>
                <td>
                    <a class="big-icon" asp-area="Management" asp-controller="Author" asp-action="Delete" asp-route-id="@item.Id"><i class="bi bi-x-circle-fill"></i></a>
                    <a class="big-icon margin-icon" asp-area="Management" asp-controller="Author" asp-action="Edit" asp-route-id="@item.Id"><i class="bi bi-pencil-square"></i></a>
                </td>
            </tr>
        }
    </tbody>
</table>

